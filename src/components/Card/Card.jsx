import React from 'react';
import './Card.css'
// import PropTypes from 'prop-types';

export const Card = () => ({
  render() {
    return (
      <div>
        CARD
      </div>
    );
  }
})

// Card.propTypes = {

// };

export default Card;