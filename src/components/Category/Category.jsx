import React from 'react';
// import PropTypes from 'prop-types';

export const Category = () => ({
  render() {
    return (
      <div>
        CATEGORIES
      </div>
    );
  }
})

// Category.propTypes = {

// };

export default Category;